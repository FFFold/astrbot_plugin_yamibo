# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **订阅更新合并转发 + 通知**：aiocqhttp 平台推送订阅更新时，楼主新楼层按每 100 层一批打包为 QQ 合并转发（每楼层一个节点：文本 + 原帖链接 + 图片），全部批次发送成功后追加一条简短更新通知（`【标题】楼主 更新了 L10-L12`，连续楼层合并为区间）；转发节点发送者 uin 取宽松值 `"0"`，不依赖协议端 self_id

### 变更

- **订阅回退发送按批直发**：不支持合并转发的平台不再逐楼层逐条发送，全部新楼层按批（≤100 楼）合并为消息直发；合并文本超过字符预算（8000 字）时自动拆批，避免超平台消息长度限制导致必然失败
- **订阅通知只发给送达成功的会话**：部分会话内容发送失败时，通知不再发给未收到内容的会话
- **订阅游标推进改为逐批**：楼层按 100/批发送，批次出现全员失败即停止，游标推进到最后一成功批次末楼层（剩余批次下轮重试）；全部批次成功后才发通知，通知发送失败不阻塞游标

## [0.2.3] - 2026-08-20

### 变更

- **漫画超限不再逐张回退**：PDF/ZIP 超过 `comic.max_file_size_mb` 时直接提示「超过大小限制，无法发送」，移除原先自动改为逐张发送前 20 张图片的回退机制（逐张发送丢失原帖排版且易刷屏，不可作为有效替代）

## [0.2.2] - 2026-08-10

### 新增

- **漫画合并转发首条消息带标题与原帖链接**：`fwd` 模式（aiocqhttp 默认方式）打包合集的第一条消息为「标题 + 原帖链接」，随后才是图片节点
- **漫画 `zip` 发送模式**：`/yamibo 漫画 <tid> zip` 以 ZIP 打包发送；`comic.deliver_mode` 配置同步支持 `zip` 选项
- **订阅图片真实推送**：订阅新楼层推送文本时同时发送楼层图片（受 `subscription.image_max` 限制），不再只提示「含图片 N 张」

### 修复

- **WAF token 刷新并发轰炸**：多调度循环共用 `asyncio.Lock`，并发刷新只求解一次；求解失败进入 60 秒退避，WAF 故障期间不再放大请求量
- **订阅漏推与游标误推进**：订阅轮询逐楼层发送；全部发送失败（或部分楼层未送达）时保留游标下轮重试，只有送达的楼层才推进基线；任一订阅 cookie 失效不再中断本轮其余订阅；发送失败逐目标记录日志
- **漫画下载静默缺页**：单张失败重试 1 次、失败计入结果并提示「N 张下载失败」；先写 `.tmp` 再原子重命名，崩溃残留的半截文件不再被复用
- **漫画临时文件过早删除**：发送后延迟清理（`comic.cleanup_delay_min`，默认 10 分钟），并只删除发送时已存在的文件，协议端异步读取不再与删除竞争
- **`deliver_mode` 配置失效**：配置值统一为 `auto|pdf|fwd|zip`，旧值 `merge_forward` 自动兼容
- **`fmt_time` 跨年误判**：当年/跨年判定改用论坛时区（东八区）年份，非东八区服务器不再出错

### 其他

- 启动初始化失败时重置运行态并记录可诊断日志（done-callback 兜底）；接入 GitHub Actions CI（pytest + ruff，Windows/Python 3.10/3.12）
- **未读提醒解析扩展**：`notices` 正则迁入 parser 并配单测，同时支持 Discuz 通用 `prompt_news_N` 导航徽标——部分模板（如当前百合会模板）静态页不再固定显示「?」
- **新增真实论坛集成测试**：`scripts/integration_test.py` 读取 AstrBot 配置中的 cookie，只读验证登录态/WAF token 并发刷新/订阅推送与游标/漫画下载（失败统计、原子写、缓存复用、延迟清理）/提醒解析/真实消息链构造（AstrBot venv）

## [0.2.1] - 2026-08-08

### 修复

- **订阅/帖子/漫画命令误报「帖子不可见」**：`parse_thread` 原按页面文本关键词（「您需要登录」「提示信息」）判断可见性，游客可浏览的帖子页回复框含「您需要登录后才可以回帖」导致误杀；现改为按楼层元素判定
- **订阅命令在倒序视图下拿不到楼主**：订阅用 `ordertype=1`（倒序）抓页，倒序第一页是最新楼层、楼主 1 楼不在其中，`author_uid` 解析为 0 导致订阅必败；现改为正序视图获取楼主信息，并用「只看楼主」视图（`authorid`）计算楼主最新楼层作为基线，与调度器轮询机制一致
- **漫画命令图片解析错乱**：同样改用正序视图，保证 `op_images()` 稳定取到主楼图片
- **登录态检查**：`订阅`/`帖子`/`漫画` 命令在 cookie 失效时给出明确提示，不再误报「帖子不可见」或「没有可下载的图片」

### 文档

- 说明全量日报与自动签到的定时抖动行为：在设定时间后 **0~5 分钟随机延迟**执行（避免每天同一时刻产生规律性请求），README 与配置 hint 同步；增量雷达无抖动，固定于榜单缓存更新后 5 分钟抓取

## [0.2.0] - 2026-08-08

### 新增

- **热帖推送重构**：信号源改为**今日热度榜**（热度排序），双通道解耦——
  - **全量日报**：每天固定时间（默认 20:00，可配置）推送一次完整今日热度榜；
  - **增量雷达**：自动对齐榜单缓存刷新（约 5 小时一次）后对比新进榜帖子并推送，当天去重、跨天重置；
  - 全量与增量可独立开关；旧配置 `hot_push.interval_min` 不再使用，改为 `hot_push.incremental.interval_min`（默认值同为 60，仅作榜单刷新时间解析失败的兜底间隔；已自定义过旧键的用户需重新在配置中设置）
- **升级注意**：热帖状态存储键更名（`hot_state` → `hot_daily_state`/`hot_incr_state`）。若在当天全量推送时间（默认 20:00）之后升级，会立即触发一次当日全量推送；旧键数据不再读取

### 修复

- **签到定时计算**：修复插件在 `sign.time` 之前启动时跳过当天签到、次日才补签的旧问题，现按当天目标时刻调度

## [0.1.1] - 2026-08-08

### 修复

- **QQ 合并转发只发单条**：aiocqhttp 适配器会把一条消息中每个 `Node` 段分别发送一次转发，导致漫画打包的每批节点被拆成多条零散转发；现每批包装为单个 `Comp.Nodes`，一次发送整批 100 节点

### 文档

- 澄清 `comic.workdir` 仅写入**生成的 PDF** 文件（容器共享卷说明），README 与 FAQ 同步

## [0.1.0] - 2026-08-08

百合会论坛助手首个正式版本。

### 功能

- **自动签到**：每日固定时间（东八区，可配置）自动打卡，支持手动签到与状态查询
- **热帖推送**：定时轮询本周回帖排行，按天去重，支持多会话绑定推送
- **漫画/连载打包**：解析帖子全部图片并生成 PDF 或 QQ 合并转发（aiocqhttp），支持页数/体积上限与超限回退
- **帖子订阅**：多对多订阅（KV 持久化），按「只看楼主」跟踪，新楼层文本+图片统一推送，失败自动重试与暂停
- **浏览与搜索**：版块帖子列表（热度/最新、简繁体名称）、全站搜索（带冷却）、帖子内容预览
- **权限分级**：签到/提醒等个人数据仅管理员可用，其余功能对所有用户开放
- **WAF 挑战自动求解**：纯 Python 自动刷新会话，无需浏览器参与

### 说明

- 需要配置论坛登录 cookie（`EeqY_2132_auth` 与 `EeqY_2132_saltkey`），获取方式见 README
- 依赖：`aiohttp`、`beautifulsoup4`、`quickjs`、`img2pdf`（Python >= 3.10，推荐 3.12）
- 支持所有 AstrBot 平台；QQ 合并转发仅 aiocqhttp，其余平台自动使用 PDF
